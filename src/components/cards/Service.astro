---
import Paragraph from '../shared/Paragraph.astro';

export interface Props{
    title:string,
    description:string,
    imageUrl:string,
    language: string
}

const {title, description, imageUrl, language} = Astro.props
---
<div class="relative p-5 sm:p-6 lg:p-8 rounded-3xl border border-box-border bg-box-bg shadow-lg shadow-box-shadow overflow-hidden">
    <div class="absolute top-0 left-0 w-full h-1/2">
        <img src={imageUrl} alt={title} class="w-full h-full object-cover pb-5"/>
    </div>
    <div class={`${language === 'es' ? 'pt-64' : 'pt-50'} mt-6 space-y-4 relative`}>
        <h2 class="text-lg md:text-xl font-semibold text-heading-2">
            {title}
        </h2>
        <Paragraph>
            {description}
        </Paragraph>
    </div>
    <span class="absolute w-32 aspect-square -bottom-16 -right-16 bg-primary/10 rounded-full"></span>
</div>
